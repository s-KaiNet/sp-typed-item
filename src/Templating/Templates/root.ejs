<%if (hasUrl) {%>import { Url } from '../Url';
<% } %><%if (hasGeoLocation) {%>import { GeoLocation } from '../Location';
<% } %>export interface <%= name %> {
    <% fields.forEach(function(field) { %>
    <%- include('Fields/' + field.template, {field: field}) %><% }); %>
}